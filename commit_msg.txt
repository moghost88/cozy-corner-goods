fix: remove unreliable MX record check from email validation

The disify.com mx field returned false for valid providers like Gmail
and Hotmail, causing a false "no mail server" error for real users.

Now only block domains the API explicitly marks as disposable.
Firebase handles actual delivery failures on its end.
