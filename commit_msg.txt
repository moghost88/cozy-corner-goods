feat: improve security, accessibility, code quality, and email validation

- AuthContext: remove dead isRecovery state and unused imports; wrap all
  functions in useCallback; memoize context value with useMemo; sanitize
  displayName; map Firebase errors to friendly messages; remove prod console.error

- Auth: remove clearRecovery/isRecovery; add password show/hide toggle;
  add autocomplete attrs; aria-live status regions; role=radio on role
  selector; aria-busy on buttons; noValidate on forms; skip-to-content link;
  real-time email validation with debounced disify.com API check +
  80+ disposable domain blocklist; live indicator on email field

- Navbar: skip-to-content link; aria-label on nav; aria-expanded and
  aria-controls on hamburger; Escape key closes menu; X icon when open;
  hidden attr on mobile panel; sr-only label on search inputs; cart
  button aria-label includes item count; mobile search bar

- ProtectedRoute: replace bare spinner with branded LoadingScreen

- NotFound: replace a href with React Router Link; proper h1 page title;
  dev-only console.warn for 404 routes

- ProductDetail: add aria-label and aria-hidden to Share button

- index.html: add X-Frame-Options SAMEORIGIN, Referrer-Policy,
  Permissions-Policy, robots meta, dns-prefetch for Firebase and Google,
  noscript font fallback, disify.com added to CSP connect-src
